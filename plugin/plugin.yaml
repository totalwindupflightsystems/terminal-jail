name: terminal-jail
version: "1.1.0"
description: >-
  Observability hooks for Hermes terminal commands: command-length
  logging, metrics export, and output annotation. Does NOT wrap or
  isolate commands. Isolation is provided by the standalone CLI
  (`standalone/terminal-jail`) and the optional systemd layer.
author: Bane + Hermes
