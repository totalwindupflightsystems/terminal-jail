# Verdict: CLN-1

**Task:** Repo hygiene: clean up project folder structure
**Evaluated:** 2026-09-11T01:41:12.697908
**Result:** ✗ FAIL

## Pipeline Stages

- ✗ **tier1**
  -   ✓ lint: 
  ✓ secrets: [90m8:37PM[0m [32mINF[0m [1mscanned ~3807605 bytes (3.81 MB) in 2.26s[0m
[90m8:37PM[0m [32m
  ✗ tests: ============================= test session starts ==============================
platform linux -- P
- ✗ **tier2**
  - INCOMPLETE
  ✓ A tracked-file inventory was made comparing root files/dirs against the documented layout (README.md, AGENTS.md, docs/); each stray or misplaced artifact (candidate: e2e-output/) is either removed, moved with git mv, or explicitly documented as an intentional exception in repo docs: Commit 13a14a4 body states the inventory explicitly: 'every tracked top-level entry compared against AGENTS.md, README.md and docs/. All 15 root files plus plugin/, standalone/, systemd/, specs/, docs/, scripts/, .github/ and skills/ are documented components.' git show --name-status 13a14a4 => M README.md, D e2e-output/report.md, D e2e-output/tasks.md. e2e-output/ removed via git rm: `git ls-files e2e-output` returns empty and the directory is absent from the working tree. README.md:281-291 documents the e2e-output retirement decision. Root listing (15 files + documented dirs) is fully accounted for in the README 'Repository layout' block at README.md:251-268.
  ✓ Commit message or docs record grep evidence that every removed/moved path is not referenced by tests (plugin/test_*.py), CI (.github/workflows/ci.yml), install.sh, or docs before the removal/move: Commit 13a14a4 body contains a 'Grep evidence, captured BEFORE any removal' section listing three commands and their results. Independently re-verified at HEAD: `grep -rn "e2e-output" plugin/` => no output; `grep -n "e2e-output" .github/workflows/ci.yml` => no output; `grep -n "e2e-output" install.sh` => no output; `grep -rn "e2e-output" docs/` => no output; `grep -n "e2e-output" plugin/test_*.py` => no output. `git grep -n "e2e-output"` matches only .coding-hermes/board/events.jsonl (board audit history) and README.md:281-286 (the new documentation text itself). README.md:281-291 also records the same grep evidence inline.
  ✗ Full test suite green after the change: .venv/bin/python -m pytest -q exits 0 with 0 failed (baseline 314 passed / 4 skipped): Ran the exact criterion command: `cd /home/kara/terminal-jail && .venv/bin/python -m pytest -q` => '12 failed, 295 passed, 13 skipped in 14.81s', EXIT=1. Criterion requires exit 0 with 0 failed; it exits 1 with 12 failed. The commit's 'pre-existing host-rule drift' claim is factually verified (pristine worktree at 13a14a4~1/70befdb yields an identical 12-failure set; diff of FAILED lines pristine vs HEAD => IDENTICAL FAILURE SETS), and with TERMINAL_JAIL_INTERRUPTOR_USER_RULES_DIR=/nonexistent TERMINAL_JAIL_INTERRUPTOR_RULES_DIR=/nonexistent the suite is green (307 passed, 13 skipped, EXIT=0) — but the criterion names the bare command with no env vars, and that command is red. The stated baseline is also not reproduced: baseline 314 passed / 4 skipped (confirmed in .coding-hermes/board/events.jsonl:240, tick 249 'suite': '314 passed, 4 skipped') vs best achievable 307 passed / 13 skipped. CI (.github/workflows/ci.yml) runs a different command (`pytest plugin/ -v -m "not integration"`) on ubuntu-latest, so CI greenness does not evidence this criterion.
  ✓ Repo docs (README.md or docs/) record the canonical layout including intentional exceptions (.vfs/, .gitreins/, .coding-hermes/, .memory-bank/, skills/, and the e2e-output decision): Committed at HEAD (verified via `git show HEAD:README.md`, not just working tree): README.md:250 '## Repository layout' with the canonical tracked tree at lines 251-268 covering plugin/, standalone/, systemd/, scripts/, specs/, docs/, skills/, .github/, .vfs/, .gitreins/, .coding-hermes/, .memory-bank/ and the 15 root files. README.md:270-279 '**Intentional exceptions**' table contains all five required entries: .vfs/ (line 275), .gitreins/ (276), .coding-hermes/ (277), .memory-bank/ (278), skills/terminal-jail-usage/ (279). README.md:281-291 '**Retired: `e2e-output/`**' records the e2e-output decision with dates, rationale, and grep evidence. AGENTS.md additionally documents repo shape (plugin/, standalone/, systemd/, specs/, .memory-bank/, .coding-hermes/board/tasks.jsonl).
Criteria 1, 2, and 4 pass with concrete evidence (inventory + git rm of e2e-output, recorded and independently re-verified grep evidence, and a committed README layout section covering all five exceptions plus the e2e-output decision), but criterion 3 fails because the exact required command `.venv/bin/python -m pytest -q` exits 1 with 12 failed / 295 passed / 13 skipped rather than exit 0 with 0 failed, and the stated 314 passed / 4 skipped baseline is not reproduced.

## Summary

Judge Result: CLN-1

Stage tier1: FAIL
    ✓ lint: 
  ✓ secrets: [90m8:37PM[0m [32mINF[0m [1mscanned ~3807605 bytes (3.81 MB) in 2.26s[0m
[90m8:37PM[0m [32m
  ✗ tests: ============================= test session starts ==============================
platform linux -- P

Stage tier2: FAIL
  INCOMPLETE
  ✓ A tracked-file inventory was made comparing root files/dirs against the documented layout (README.md, AGENTS.md, docs/); each stray or misplaced artifact (candidate: e2e-output/) is either removed, moved with git mv, or explicitly documented as an intentional exception in repo docs: Commit 13a14a4 body states the inventory explicitly: 'every tracked top-level entry compared against AGENTS.md, README.md and docs/. All 15 root files plus plugin/, standalone/, systemd/, specs/, docs/, scripts/, .github/ and skills/ are documented components.' git show --name-status 13a14a4 => M README.md, D e2e-output/report.md, D e2e-output/tasks.md. e2e-output/ removed via git rm: `git ls-files e2e-output` returns empty and the directory is absent from the working tree. README.md:281-291 documents the e2e-output retirement decision. Root listing (15 files + documented dirs) is fully accounted for in the README 'Repository layout' block at README.md:251-268.
  ✓ Commit message or docs record grep evidence that every removed/moved path is not referenced by tests (plugin/test_*.py), CI (.github/workflows/ci.yml), install.sh, or docs before the removal/move: Commit 13a14a4 body contains a 'Grep evidence, captured BEFORE any removal' section listing three commands and their results. Independently re-verified at HEAD: `grep -rn "e2e-output" plugin/` => no output; `grep -n "e2e-output" .github/workflows/ci.yml` => no output; `grep -n "e2e-output" install.sh` => no output; `grep -rn "e2e-output" docs/` => no output; `grep -n "e2e-output" plugin/test_*.py` => no output. `git grep -n "e2e-output"` matches only .coding-hermes/board/events.jsonl (board audit history) and README.md:281-286 (the new documentation text itself). README.md:281-291 also records the same grep evidence inline.
  ✗ Full test suite green after the change: .venv/bin/python -m pytest -q exits 0 with 0 failed (baseline 314 passed / 4 skipped): Ran the exact criterion command: `cd /home/kara/terminal-jail && .venv/bin/python -m pytest -q` => '12 failed, 295 passed, 13 skipped in 14.81s', EXIT=1. Criterion requires exit 0 with 0 failed; it exits 1 with 12 failed. The commit's 'pre-existing host-rule drift' claim is factually verified (pristine worktree at 13a14a4~1/70befdb yields an identical 12-failure set; diff of FAILED lines pristine vs HEAD => IDENTICAL FAILURE SETS), and with TERMINAL_JAIL_INTERRUPTOR_USER_RULES_DIR=/nonexistent TERMINAL_JAIL_INTERRUPTOR_RULES_DIR=/nonexistent the suite is green (307 passed, 13 skipped, EXIT=0) — but the criterion names the bare command with no env vars, and that command is red. The stated baseline is also not reproduced: baseline 314 passed / 4 skipped (confirmed in .coding-hermes/board/events.jsonl:240, tick 249 'suite': '314 passed, 4 skipped') vs best achievable 307 passed / 13 skipped. CI (.github/workflows/ci.yml) runs a different command (`pytest plugin/ -v -m "not integration"`) on ubuntu-latest, so CI greenness does not evidence this criterion.
  ✓ Repo docs (README.md or docs/) record the canonical layout including intentional exceptions (.vfs/, .gitreins/, .coding-hermes/, .memory-bank/, skills/, and the e2e-output decision): Committed at HEAD (verified via `git show HEAD:README.md`, not just working tree): README.md:250 '## Repository layout' with the canonical tracked tree at lines 251-268 covering plugin/, standalone/, systemd/, scripts/, specs/, docs/, skills/, .github/, .vfs/, .gitreins/, .coding-hermes/, .memory-bank/ and the 15 root files. README.md:270-279 '**Intentional exceptions**' table contains all five required entries: .vfs/ (line 275), .gitreins/ (276), .coding-hermes/ (277), .memory-bank/ (278), skills/terminal-jail-usage/ (279). README.md:281-291 '**Retired: `e2e-output/`**' records the e2e-output decision with dates, rationale, and grep evidence. AGENTS.md additionally documents repo shape (plugin/, standalone/, systemd/, specs/, .memory-bank/, .coding-hermes/board/tasks.jsonl).
Criteria 1, 2, and 4 pass with concrete evidence (inventory + git rm of e2e-output, recorded and independently re-verified grep evidence, and a committed README layout section covering all five exceptions plus the e2e-output decision), but criterion 3 fails because the exact required command `.venv/bin/python -m pytest -q` exits 1 with 12 failed / 295 passed / 13 skipped rather than exit 0 with 0 failed, and the stated 314 passed / 4 skipped baseline is not reproduced.

Overall: FAIL ✗
